<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <div>
      <% messages.forEach(function(m) { %>
      <div><%= m.text %></div>
      <div><%= m.user %></div>
      <div><%= m.added %> <% }); %></div>
    </div>
    <a href="/new">form</a>
  </body>
</html>
